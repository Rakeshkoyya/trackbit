# ===========================================
# Trackbit Master Environment Configuration
# ===========================================

# ===================
# Required Variables
# ===================

# JWT secret key for authentication (minimum 32 characters)
JWT_SECRET_KEY=your-super-secret-jwt-key-min-32-chars

# ===================
# Optional Variables
# ===================

# Enable debug mode (default: false)
DEBUG=false

# Number of Uvicorn workers (default: 2)
WORKERS=2

# ===================
# Database (auto-configured)
# ===================
# The DATABASE_URL is automatically set in docker-compose.yml
# pointing to the containerized PostgreSQL instance.
# Only override this if using an external database:
# DATABASE_URL=postgresql://user:password@host:5432/database
